{
  "scripts": {
    "start": "serve -s src/",
    "test": "jest --coverage"
  },
  "dependencies": {
    "@babel/core": "^7.18.0",
    "@babel/preset-env": "^7.18.0",
    "babel-jest": "^28.1.0",
    "firebase": "^9.8.1",
    "serve": "^13.0.2"
  },
  "devDependencies": {
    "jest": "^28.1.0"
  }
}
