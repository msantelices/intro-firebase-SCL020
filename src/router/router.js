
const routes = [
    {
        path: '/',
        template: Home()
    },
    {
        path: '/login',
        template: Login()
    }
]

// Function que use el history API para cambiar de ruta en base al array anterior: Tip investigar Filter

// Function que use el history API para volver atras